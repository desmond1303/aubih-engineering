
import { isLocalLink } from 'engineering/helpers/is-local-link';
import { module, test } from 'qunit';

module('Unit | Helper | is local link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLocalLink([42]);
  assert.ok(result);
});

