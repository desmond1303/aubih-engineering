
import { getCol } from 'engineering/helpers/get-col';
import { module, test } from 'qunit';

module('Unit | Helper | get col');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCol([42]);
  assert.ok(result);
});

